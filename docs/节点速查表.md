# Tikpan Pro 节点速查表

> 更新日期：2026-06-09  
> 当前口径：根目录 `__init__.py` 已注册 52 个节点。  
> 用途：快速查找每个节点的能力、关键输入和输出，方便 ComfyUI 使用，也方便后续 Web 网站做表单和计费映射。

## 阅读顺序

1. 只想知道该用哪个能力：先看本表按图片、视频、音频、分析、工具分组的节点行。
2. 想照着搭工作流：看 [节点使用教程](节点使用教程.md)。
3. 想维护菜单分组：看 [节点功能分类](Tikpan_ComfyUI_节点功能分类.md)。
4. 想确认版本变更：看根目录 [CHANGELOG](../CHANGELOG.md)。

## 图片节点

| 节点 | 主要用途 | 关键输入 | 输出 |
| --- | --- | --- | --- |
| `TikpanGptImage2OfficialNode` | GPT-Image-2 官方生图 | `API_密钥`、`生成指令`、`分辨率档位`、`画面比例`、`画质与推理强度`、`审核强度`、`seed` | `IMAGE`、日志 |
| `TikpanGptImage2OfficialEditV2` | GPT-Image-2 官方修图 | `主图像`、`编辑指令`、`生成张数`、`分辨率档位`、`画面比例`、`画质`、`背景模式`、`遮罩掩码`、参考图 | `IMAGE`、日志 |
| `TikpanGptImage2GenNode` | GPT-Image-2-all 多参考图生成 | `API_密钥`、`生成指令`、`分辨率档位`、`画面比例`、`品质`、`seed`、参考图 1-14 | `IMAGE`、日志 |
| `TikpanGptImage2EditNode` | GPT-Image-2-all 修图 | `底图`、`修改指令`、`品质`、`遮罩_Mask`、`产品参考图` | `IMAGE`、日志 |
| `TikpanGeminiImageMaxNode` | Gemini 14 图生成/融合 | `API_密钥`、`修改指令`、`模型`、`分辨率`、`画面比例`、`调用方式`、参考图 1-14 | `IMAGE`、日志 |
| `TikpanNanoBananaProNode` | Nano Banana Pro 图像生成 | `API_密钥`、`调用方式`、`模型`、`修改指令`、`分辨率`、`画面比例`、`温度`、`max_tokens`、参考图 1-14 | `IMAGE`、日志 |
| `TikpanDoubaoImageNode` | 豆包图像生成 | `api_key`、`prompt`、`size_mode`、`size_quality`、`aspect_ratio`、`output_format`、`watermark`、`input_image`、`image_url_or_base64` | `IMAGE`、日志 |
| `TikpanGptImage2Node` | 旧版 GPT-Image-2 简化生图 | `API_密钥`、`提示词`、`模型`、`尺寸`、`品质`、`风格`、`seed` | `IMAGE`、日志 |
| `TikpanGrokImagineImageNode` | Grok Imagine Image 文生图 | `API_密钥`、`生成指令`、`模型`、`生成张数`、`画面比例`、`清晰度`、`兼容尺寸`、`返回方式`、`跳过错误` | `IMAGE`、日志 |
| `TikpanGrokImagineImageProNode` | Grok Imagine Image Pro 文生图（高清） | 同上，模型固定 `grok-imagine-image-pro` | `IMAGE`、日志 |
| `TikpanGrokImagineImageEditNode` | Grok Imagine Image 参考图生图/修图 | `API_密钥`、`参考图1-3`、`编辑指令`、`模型`、`生成张数`、`画面比例`、`清晰度` | `IMAGE`、日志 |
| `TikpanGrokImagineImageProEditNode` | Grok Imagine Image Pro 参考图/修图 | 同上，模型固定 Pro | `IMAGE`、日志 |
| `TikpanQwenImage20Node` | Qwen-Image-2.0 文生图/图生图 | `API_密钥`、`生成指令`、`模型`、`生成张数`、`生成模式`、`画面比例`、`清晰度`、`画质策略`、`随机种子`、参考图 1-4 | `IMAGE`、日志 |
| `TikpanWan27ImageProNode` | Wan 2.7 Image Pro 文生图/编辑 | `API_密钥`、`生成指令`、`生成模式`、`画面比例`、`清晰度(支持4K)`、`画质策略`、`思考模式`、参考图 1-4 | `IMAGE`、日志 |

## 视频节点

| 节点 | 主要用途 | 关键输入 | 输出 |
| --- | --- | --- | --- |
| `TikpanHappyHorseT2VNode` | HappyHorse 文生视频 | `api_key`、`prompt`、`mode`、`resolution`、`ratio`、`duration`、`watermark`、`seed` | 本地路径、任务 ID、云端链接、日志、`VIDEO` |
| `TikpanHappyHorseI2VNode` | HappyHorse 图生视频 | `api_key`、`prompt`、`mode`、`resolution`、`duration`、`首帧图片` 或 `图片URL` | 本地路径、任务 ID、云端链接、日志、`VIDEO` |
| `TikpanHappyHorseR2VNode` | HappyHorse 参考图生视频 | `api_key`、`prompt`、`mode`、`resolution`、`duration`、参考图 1-9 或图片 URL 列表 | 本地路径、任务 ID、云端链接、日志、`VIDEO` |
| `TikpanHappyHorseVideoEditNode` | HappyHorse 视频编辑 | `api_key`、`prompt`、`mode`、`视频URL` 或 `本地视频`、参考图 1-5 | 本地路径、任务 ID、云端链接、日志、`VIDEO` |
| `TikpanGrokVideoNode` | Grok-Videos 视频生成 | `api_key`、`prompt`、`model`、`duration`、`aspect_ratio`、参考图 1-4 | 本地路径、云端链接、日志、`VIDEO` |
| `TikpanExclusiveVideoNode` | 旧版 Grok3 视频生成 | `Tikpan_API密钥`、`Grok3专属提示词`、`模型选择`、`比例`、`分辨率`、`seed`、参考图、`校验HTTPS证书` | 本地路径、任务 ID、云端链接、日志、`VIDEO` |
| `TikpanVeoVideoNode` | Veo 3.1 多模型视频生成 | `API_密钥`、`Veo专属提示词`、`模型选择`、`比例(16:9/9:16)`、`seed`、可选首帧/尾帧图、components 垫图 1-3、`校验HTTPS证书` | 本地路径、任务 ID、云端链接、日志、`VIDEO` |
| `TikpanTaskFetcherNode` | 异步任务查询与下载 | `api_key`、`task_id`、`file_prefix`、`max_wait_seconds`、`poll_interval` | 本地路径、任务 ID、云端链接、日志、`VIDEO` |
| `TikpanKlingMotionControlNode` | Kling 动作控制视频生成（v2.6/v3） | `API_密钥`、`角色图像`、`动作参考视频URL`、`动作描述`、`模型版本与模式`、`视频时长`、`画面比例`、可选`本地动作视频路径`、`最长等待秒数`；请求使用 `image_url` / `video_url` | 本地路径、任务 ID、云端链接、日志、`VIDEO` |
| `TikpanMiniMaxHailuoVideoNode` | MiniMax Hailuo 视频生成 | `API_密钥`、`生成模式`、`模型版本`（Hailuo-2.3 / Hailuo-02）、`生成指令`、`视频时长`、`分辨率`、可选首帧/尾帧图；首尾帧仅支持 Hailuo-02，1080P 仅支持 6 秒 | 本地路径、任务 ID、云端链接、日志、`VIDEO` |
| `TikpanKlingText2VideoNode` | Kling 文生视频 | `API_密钥`、`模型版本`（v2.5 Turbo / v2.6 / v3）、`生成指令`、`模式`、`视频时长`、`画面比例`、`负向提示词`、`CFG Scale`、`生成声音`（仅 v2.6+） | 本地路径、任务 ID、云端链接、日志、`VIDEO` |
| `TikpanKlingImage2VideoNode` | Kling 图生视频 | `API_密钥`、`首帧图`、`模型版本`、`生成指令`、`模式`、`视频时长`、`画面比例`、可选`尾帧图`；尾帧图仅支持 5 秒 | 本地路径、任务 ID、云端链接、日志、`VIDEO` |
| `TikpanVidu3ReferenceVideoNode` | Vidu3 多参考图生视频 | `API_密钥`、`生成指令`、`参考图1-7`、`视频时长`、`清晰度`、`画面比例`、`随机种子`、`音画同步`、`音频类型`、`最长等待秒数` | 本地路径、任务 ID、云端链接、日志、`VIDEO` |
| `TikpanVidu3TurboVideoNode` | Vidu3 Turbo 文生/图生/首尾帧 | `API_密钥`、`生成指令`、`生成模式`、`视频时长`、`清晰度`、`画面比例`、`运动幅度`、`随机种子`、可选`首帧图`/`尾帧图` | 本地路径、任务 ID、云端链接、日志、`VIDEO` |
| `TikpanGeminiOmniVideoNode` | Gemini Omni Flash 视频生成 | `API_密钥`、`生成指令`、`模型`（omni-flash / omni-flash-components）、`视频时长`、`画面比例`、`清晰度`、`生成原生音频`、`随机种子`、`负面提示词`、参考图 1-5（仅 components 模型） | 本地路径、任务 ID、云端链接、日志、`VIDEO` |

## 音频节点

| 节点 | 主要用途 | 关键输入 | 输出 |
| --- | --- | --- | --- |
| `TikpanSunoMusicNode` | Suno 音乐生成 | `API_密钥`、`生成模式`、`歌曲标题`、`创作提示词`、`风格标签`、`模型版本`、`生成纯音乐`、续写/Persona 参数 | 音频路径、音频链接、任务 ID、片段 ID、标题、标签、`AUDIO` 1、`AUDIO` 2 |
| `TikpanMiniMaxSpeech28HDNode` | speech-2.8-hd 高清语音合成 | `API_密钥`、`合成文本`、`调用方式`、`音色` 下拉、`自定义voice_id`、`语言增强`、`语速`、`音量`、`音调`、`情绪`、`音频格式`、`POST重试策略`、发音字典/音色混合/字幕参数 | 音频路径、音频链接、任务 ID、文件 ID、计费字符数、状态日志、`AUDIO` |
| `TikpanMiniMaxSpeech28TurboNode` | speech-2.8-turbo 极速语音合成 | `API_密钥`、`合成文本`、`调用方式`、`音色` 下拉、`自定义voice_id`、`语言增强`、`语速`、`音量`、`音调`、`情绪`、`音频格式`、`POST重试策略`、发音字典/音色混合/字幕参数 | 音频路径、音频链接、任务 ID、文件 ID、计费字符数、状态日志、`AUDIO` |
| `TikpanDoubaoTTS20Node` | 豆包语音合成 2.0 | `API_密钥`、`合成文本`、`音色` 下拉、`自定义voice_type`、`语速`、`音量`、`音调`、`情感`、`音频格式`、`资源ID`、`POST重试策略` | 音频路径、音频链接、voice_type、计费字符数、状态日志、`AUDIO` |
| `TikpanGemini31FlashTTSNode` | Gemini 3.1 Flash TTS Preview | `API_密钥`、`合成文本`、`调用方式`、`音色` 下拉、`自定义voice_name`、`语气指令`、`语言代码`、`采样率`、`POST重试策略`、高级 JSON | 音频路径、音频链接、输入输出用量、接口路径、状态日志、`AUDIO` |

## 分析与提示词节点

| 节点 | 主要用途 | 关键输入 | 输出 |
| --- | --- | --- | --- |
| `TikpanGeminiVideoAnalystNode` | 音视频分析、镜头拆解 | `Tikpan_API密钥`、`视频流_IMAGE`、`视频帧率_FPS`、`分析模型`、可选 `音频流_AUDIO`、`特定关注点`、`校验HTTPS证书` | 分析报告 |
| `TikpanGemini3FlashPreviewAnalystNode` | Gemini 3 Flash Preview 图片/视频理解分析 | `API_密钥`、`模型`、`分析任务`、`分析要求`、`输出格式`、图片 1-4、`图片URL列表`、`抽帧策略`、`视频输入策略`、`视频帧_IMAGE`、`本地视频路径`、`视频URL`、`URL错误处理`、`POST重试策略` | 分析报告、反推提示词、结构化 JSON、用量、状态日志 |
| `TikpanGPT5MiniResponsesNode` | GPT-5.4 Mini 多模态推理与图片/文件分析 | `API_密钥`、`模型`、`任务类型`、`用户问题`、`系统指令`、`输出格式`、`reasoning_effort`、图片 1-4、`图片URL列表`、`抽帧策略`、`视频帧_IMAGE`、`文件URL列表`、`本地文件路径`、`启用联网搜索`、`URL错误处理`、`POST重试策略` | 回答文本、优化提示词、结构化 JSON、用量、状态日志 |
| `TikpanGemini35FlashNode` | Gemini 3.5 Flash 长文档/多模态推理 | `API_密钥`、`模型`、`调用方式`、`任务类型`、`用户问题`、`系统指令`、`输出格式`、`thinking`、图片 1-6、`图片URL列表`、`本地文件路径`、`POST重试策略` | 回答文本、结构化 JSON、用量、状态日志 |
| `TikpanGrokPromptOptimizerNode` | 把拆解报告重构成视频提示词 | `Tikpan_API密钥`、`文本处理模型`、`Gemini原片拆解报告`、`核心产品与植入场景`、`氛围与运镜微调`、`校验HTTPS证书` | Grok 专属提示词、重构日志 |
| `TikpanPromptsManagerNode` | 同步 9 个 GitHub 提示词仓库，可选启用中文翻译并缓存 | `操作`（查看状态/同步全部仓库）、`同步单个仓库`、`Tikpan_API密钥`（选填，启用翻译）、`翻译模型`（deepseek-v4-flash） | 状态报告 |
| `TikpanPromptsSelectorNode` | 从全部 503 张卡片选择（优先显示中文标题+预览） | `选择提示词`（编号+标题+预览）、`过滤_仓库`、`过滤_标签`、`搜索关键词`、`显示详情` | 提示词文本（英文原文）、卡片标题、卡片详情 |
| `TikpanPromptsImageSelectorNode` | 只显示图片类卡片（336 张，覆盖 GPT-Image-2 / Nano Banana） | 同上，但下拉、仓库、标签都已按 `image` 标签预过滤 | 同上 |
| `TikpanPromptsVideoSelectorNode` | 只显示视频类卡片（163 张，覆盖 Seedance） | 同上，但已按 `video` 标签预过滤 | 同上 |
| `TikpanPromptsSearchNode` | 关键词搜索提示词（中英双语都匹配） | `搜索关键词`、`过滤_仓库`、`过滤_标签`、`最多返回数量`、`结果索引` | 提示词文本、卡片标题、搜索结果列表、结果总数 |

## 工具节点

| 节点 | 主要用途 | 关键输入 | 输出 |
| --- | --- | --- | --- |
| `TikpanParallelImageEngineNode` | API 多模型并发/容灾生图 | `API_Key`、`Prompt`、`Models`（每行一个模型）、`Strategy`（failover/race_first_success/parallel_all）、`Max_Concurrency`、`Images_Per_Model` | `Images`、`Stage_Log`、`Result_JSON` |
| `TikpanAsyncImageSubmitNode` | 异步提交图片任务并立即返回 | `API_Key`、`Model`、`Prompt`、可选参考图 | `Task_ID`、`Stage_Log`、`Task_JSON` |
| `TikpanAsyncImageResultNode` | 查询单个异步任务结果 | `API_Key`、`Task_ID`、`Wait_Mode`（return_now/wait_until_done）、`Max_Wait_Seconds` | `Images`、`Stage_Log`、`Task_JSON` |
| `TikpanAsyncImageJoinNode` | 合并多个异步任务，批量等待 | `Task_ID_1~8`、`Wait_All`、`Max_Wait_Seconds`、`Poll_Interval_Seconds` | `Images`、`Stage_Log`、`Tasks_JSON` |
| `TikpanAsyncTaskListNode` | 查看当前进程的最近异步任务 | 无必填参数 | `Tasks_JSON`、`Stage_Log` |

## PSD 节点

| 节点 | 主要用途 | 关键输入 | 输出 |
| --- | --- | --- | --- |
| `TikpanSmartPSDLayeringNode` | 智能分层 PSD（主体/装饰/文字/二维码/色块自动分离） | `输入图片`、`文件名`、`分层档位`（经济/标准/极致）、`场景类型`、`补全被遮挡区域`、`检测文字`、`最小元素面积`、`边缘羽化`、`自动安装依赖` | `PSD文件路径`、`分层日志`、`预览图` |
| `TikpanPSDDependencyDownloaderNode` | 预下载分层节点所需的依赖和 AI 模型 | `下载档位`（经济/标准/极致/全部）、`包含补全模型` | `下载日志`、`状态预览` |

### 三档分层精度对比

| 档位 | 模型组合 | 下载体积 | 单图耗时 | 效果 | 推荐场景 |
| --- | --- | --- | --- | --- | --- |
| 经济档 | BiRefNet + OpenCV 连通域 + PaddleOCR | 按需下载 | 5-15秒 | ⭐⭐⭐ | 简单商品图、追求速度 |
| 标准档（推荐） | BiRefNet + SAM2 自动多尺度 + PaddleOCR | 按需下载 | 20-60秒 | ⭐⭐⭐⭐ | 大多数日常场景、复杂商品图、海报 |
| 极致档 | BiRefNet + GroundingDINO + SAM2 + LaMa | 按需下载 | 60-180秒 | ⭐⭐⭐⭐⭐ | 商业级 PSD、复杂海报、被遮挡区域补全 |

> 标准档开启 `补全被遮挡区域=是` 会额外启用补全模型；实际耗时取决于图片尺寸、显卡和首次模型下载状态。

## 网站化字段映射建议

| 后台表 | 建议字段 |
| --- | --- |
| 模型表 `models` | `model_key`、`display_name`、`category`、`provider`、`supports_image`、`supports_video`、`supports_audio`、`enabled` |
| 参数表 `model_parameters` | `model_key`、`field_key`、`label`、`type`、`required`、`default_value`、`options_json`、`sort_order` |
| 供应商渠道表 `provider_channels` | `provider`、`base_url`、`api_key_encrypted`、`priority`、`weight`、`enabled`、`timeout_seconds` |
| 价格表 `pricing_rules` | `model_key`、`unit_type`、`base_cost`、`user_price`、`currency`、`resolution_multiplier`、`duration_multiplier` |
| 任务表 `generation_tasks` | `task_id`、`user_id`、`model_key`、`status`、`request_json`、`provider_task_id`、`cost_amount`、`result_url`、`error_message` |
| 用户余额流水表 `wallet_ledger` | `user_id`、`task_id`、`amount`、`type`、`balance_after`、`remark` |

核心原则：ComfyUI 里的节点参数可以直接变成网站表单字段，但供应商密钥、渠道选择、真实成本和扣费逻辑必须放在后台。
